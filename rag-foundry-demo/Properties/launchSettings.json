{
  "profiles": {
    "RagFoundryDemo": {
      "commandName": "Project",
      "environmentVariables": {
        "//1": "Restrict DefaultAzureCredential to developer creds (az login) — this VM has a managed identity that would otherwise be picked first.",
        "AZURE_TOKEN_CREDENTIALS": "dev",
        "//2": "Pin the tenant that owns the demo resources (VS_Sub_MRL). Your default az context is a different tenant, so without this the dev creds request a token for the wrong tenant (HTTP 400 tenant mismatch).",
        "AZURE_TENANT_ID": "5509d93f-af21-4739-b7c9-ec32c2ca43a1"
      }
    }
  }
}
