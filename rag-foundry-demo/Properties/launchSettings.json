{
  "profiles": {
    "RagFoundryDemo": {
      "commandName": "Project",
      "environmentVariables": {
        "//1": "Restrict DefaultAzureCredential to developer creds (az login) — this VM has a managed identity that would otherwise be picked first.",
        "AZURE_TOKEN_CREDENTIALS": "dev",
        "//2": "Set this to the tenant that owns your demo resources if your default az context is a different tenant — otherwise the dev creds request a token for the wrong tenant (HTTP 400 tenant mismatch). Leave blank to use your current az context.",
        "AZURE_TENANT_ID": ""
      }
    }
  }
}
